---
import Header from "../components/app/Header.astro";
import UserAside from "../components/app/UserAside";
import ImageFondo from "../assets/mascota_happy.png";
import { Image } from "astro:assets";
import "../styles/global.css";
---

<div class="min-h-screen w-full flex flex-col overflow-x-hidden">
  <Header />

  <section class="flex min-h-0 flex-1 flex-col lg:flex-row">
    <aside class="flex w-full shrink-0 flex-row items-center gap-4 border-b border-black/10 bg-ALI-600 p-4 lg:w-72 lg:flex-col lg:items-stretch lg:border-b-0 lg:border-r">
      <div class="w-full">
        <UserAside client:load />
      </div>

      <figure class="flex w-24 shrink-0 items-center justify-center sm:w-32 lg:mt-2 lg:w-full">
        <Image
          src={ImageFondo}
          alt="Mascota EnergX"
          class="h-auto w-full max-w-full object-contain"
        />
      </figure>
    </aside>

    <div class="min-w-0 flex-1">
      <slot />
    </div>
  </section>
</div>